{% extends 'base.html.twig' %}

{% block body %}
   <h2>Sign in</h2>
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}
    <a href="/forgotpassword" class="btn btn-link">Forgot your password?</a>
{% endblock %}
